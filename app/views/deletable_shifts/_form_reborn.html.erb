<h1><%= @event.id %></h1>

<div class="row">
    <%= form_with(model: @event, url: reborn_deletable_shift_path(@event), html: { method: :patch }, remote: true) do |f| %>
        <%= f.submit "元に戻す", class: "btn btn-primary" %>
    <% end %>
</div>    