<div class="row">
      <%= form_with(model: @event, url: fill_in_perfect_shift_path(@event), html: {method: :patch},remote: true) do |f| %>
         <%= f.submit "登録",   class: "btn btn-primary js-event-create-btn" %>
      <% end %>
</div>        